services:
  diffcheck:
    build:
      context: .
      dockerfile: DiffCheck.Web/Dockerfile
    # Or pull the published image instead of building locally:
    # image: ghcr.io/milvasic/diffcheck-web:latest
    ports:
      - "8080:8080"
